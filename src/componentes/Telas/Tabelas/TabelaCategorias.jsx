import { Button, Container, Table } from "react-bootstrap";

export default function TabelaCategorias (props) {
    return (
        <>
            <Container>
                <Button className="mb-3" variant="primary">Adicionar</Button>
                <Table>

                </Table>
            </Container>
        </>
    );
}